apiVersion: apps/v1
kind: Deployment
metadata:
  name: atdsal
  namespace: insahi
spec:
  replicas: 3
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: insahi-atdsal
  template:
    metadata:
      labels:
        app: insahi-atdsal
    spec:
      containers:
        - name: atdsal
          image: daul0519/atdsal-service:7b0f50a
          ports:
            - containerPort: 1000
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"



---


apiVersion: v1
kind: Service
metadata:
  name: atdsal-service
  namespace: insahi

spec:
  type: ClusterIP
  selector:
    app: insahi-atdsal

  ports:
    - port: 1000
      targetPort: 1000
